Si read n'a pas lu 0 caractere 
	chaine creee avec read	
substr jusqua \n
garder le positionnement avec variable static
retourner substr de la chaine lue 




check if Buffer is non empty
	empty->read line
	non_empty-> copy buffer first
	
for char in buffer
	ligne_de_retour += char
	if (end of file or line)
		move buffer
		retourne line
	
if buffer all read
	read buffer



new line = strlen(ancienne ligne a partir de \n) + strlen(nouvelle ligne jusqua \n)

fonction qui realloue 

fin de fonction 

si \n 
	copie de l'ancienne chaine dans la nouvelle

read
si vide
on renvoie l'ancienne
si pas vide et ancienne pas vide  
	on copie l'ancienne et la nouvelle dans une chaine
sinon on substr la nouvelle

get_backslash
get_line
